import { useMemo, useState, useSyncExternalStore, type ReactNode, type SetStateAction } from 'react'
import { createFileRoute } from '@tanstack/react-router'
import { Activity, ArrowRight, Bell, CalendarDays, Check, CheckCircle2, ChevronRight, Clock3, ExternalLink, FileText, FileUp, HeartPulse, Info, Leaf, Link2, Mail, Menu, Moon, Pill, Plus, Search, ShieldAlert, Smartphone, Sparkles, Upload, UserRound, Weight, X } from 'lucide-react'
import { Button } from '@/components/ui/button'
import Model, { type IExerciseData, type IMuscleStats, type Muscle } from 'react-body-highlighter'
import { BlinkClientBoundary } from '@/components/BlinkClientBoundary'

type Tab = 'Hoy' | 'Mi Salud' | 'Cuerpo' | 'Historia' | 'VITAL' | 'Yo'
const tabs: { name: Tab; icon: typeof Leaf }[] = [
  { name: 'Hoy', icon: Leaf }, { name: 'Mi Salud', icon: HeartPulse }, { name: 'Cuerpo', icon: Activity }, { name: 'Historia', icon: FileText }, { name: 'VITAL', icon: Sparkles }, { name: 'Yo', icon: UserRound },
]

const vitalSeenSubscribe = (callback: () => void) => {
  window.addEventListener('vital-demo-seen', callback)
  return () => window.removeEventListener('vital-demo-seen', callback)
}

const getFirstVisit = () => !localStorage.getItem('vital-demo-seen')
const getServerFirstVisit = () => false

function useStoredState<T>(key: string, fallback: T) {
  const fallbackSnapshot = JSON.stringify(fallback)
  const subscribe = (callback: () => void) => {
    window.addEventListener(`vital-storage:${key}`, callback)
    return () => window.removeEventListener(`vital-storage:${key}`, callback)
  }
  const snapshot = useSyncExternalStore(
    subscribe,
    () => localStorage.getItem(key) ?? fallbackSnapshot,
    () => fallbackSnapshot,
  )
  const value = useMemo(() => JSON.parse(snapshot) as T, [snapshot])
  const setValue = (next: SetStateAction<T>) => {
    const resolved = typeof next === 'function' ? (next as (current: T) => T)(value) : next
    localStorage.setItem(key, JSON.stringify(resolved))
    window.dispatchEvent(new Event(`vital-storage:${key}`))
  }
  return [value, setValue] as const
}

function downloadCalendarEvent() {
  const event = ['BEGIN:VCALENDAR', 'VERSION:2.0', 'BEGIN:VEVENT', 'SUMMARY:Cita con Dra. Laura Soler', 'DTSTART:20260923T083000Z', 'DTEND:20260923T093000Z', 'LOCATION:CAP Les Corts', 'DESCRIPTION:Preparar la visita desde VITAL OS.', 'END:VEVENT', 'END:VCALENDAR'].join('\\r\\n')
  const url = URL.createObjectURL(new Blob([event], { type: 'text/calendar;charset=utf-8' }))
  const link = document.createElement('a')
  link.href = url
  link.download = 'cita-vital.ics'
  link.click()
  URL.revokeObjectURL(url)
}

function TabPanel({ tab, onBack }: { tab: Exclude<Tab, 'Hoy' | 'Yo'>; onBack: () => void }) {
  const [savedNote, setSavedNote] = useStoredState('vital-daily-note', '')
  const [visitNote, setVisitNote] = useStoredState('vital-visit-note', '')
  const [body, setBody] = useStoredState('vital-body-checkin', { energy: '', sleep: '', symptoms: '' })
  const [symptoms, setSymptoms] = useStoredState<VitalSymptom[]>('vital-symptoms', [])
  const [confirmedFile] = useStoredState<string | null>('vital-confirmed-document', null)
  const content = {
    'Mi Salud': { icon: HeartPulse, eyebrow: 'Tu resumen', title: 'Una visión tranquila de lo importante.', text: 'Aquí reuniremos tus informes, medicación, citas y señales de salud confirmadas por ti.', items: ['Medicación actual', 'Próximas citas', confirmedFile ? `Documento: ${confirmedFile}` : 'Informes revisados'] },
    'Cuerpo': { icon: Activity, eyebrow: 'Escúchalo', title: 'Tu cuerpo también habla.', text: 'Anota cómo duermes, tu energía o cualquier molestia sin convertirlo en una obligación.', items: ['Energía: pendiente', 'Sueño: pendiente', 'Síntomas: ninguno añadido'] },
    'Historia': { icon: FileText, eyebrow: 'Tu recorrido', title: 'Tu historia, ordenada y entendible.', text: 'Los documentos que confirmes podrán aparecer aquí con su fecha y fuente, siempre bajo tu control.', items: [confirmedFile ? `Documento confirmado: ${confirmedFile}` : 'Ningún documento confirmado todavía'] },
    VITAL: { icon: Sparkles, eyebrow: 'Tu acompañante', title: '¿Qué necesitas hoy?', text: 'Habla con VITAL sobre tu medicación, tus síntomas o cómo preparar tu próxima visita.', items: ['Conversación privada en este dispositivo', 'Respuestas claras y sin juicios', 'Preparación de tu cita'] },
  }[tab]
  const Icon = content.icon
  const bodyOptions = {
    energy: ['Con energía', 'Normal', 'Bajo'],
    sleep: ['He descansado', 'Regular', 'He dormido mal'],
    symptoms: ['Ninguno', 'Algo leve', 'Necesito revisarlo'],
  } as const
  const bodyItems = [
    `Energía: ${body.energy || 'sin registrar'}`,
    `Sueño: ${body.sleep || 'sin registrar'}`,
    `Síntomas: ${body.symptoms || 'ninguno añadido'}`,
  ]
  return <div className="fixed inset-0 z-40 overflow-y-auto bg-background px-5 pb-28 pt-8 lg:px-10"><div className="mx-auto max-w-4xl"><button type="button" onClick={onBack} className="mb-10 flex items-center gap-2 text-sm font-semibold text-primary hover:underline"><ArrowRight className="size-4 rotate-180" /> Volver a Hoy</button><div className="grid gap-8 lg:grid-cols-[0.8fr_1.2fr] lg:items-start"><div><span className="grid size-14 place-items-center rounded-2xl bg-secondary text-primary"><Icon className="size-7" /></span><p className="mt-8 text-sm font-bold uppercase tracking-[0.2em] text-primary">{content.eyebrow}</p><h1 className="mt-3 font-serif text-4xl leading-tight text-primary sm:text-6xl">{content.title}</h1><p className="mt-5 max-w-xl text-base leading-7 text-muted-foreground">{content.text}</p>{tab === 'Cuerpo' && <div className="mt-8"><BodyVisual body={body} symptoms={symptoms} setSymptoms={setSymptoms} /></div>}</div><div className="rounded-[2rem] border border-border bg-card p-6 shadow-sm sm:p-8"><p className="text-sm font-semibold text-primary">Tu espacio</p>{tab === 'Cuerpo' && <div className="mt-5 space-y-5"><p className="text-sm leading-6 text-muted-foreground">Tres preguntas rápidas. Elige la respuesta que más se parezca a hoy.</p>{(Object.entries(bodyOptions) as [keyof typeof bodyOptions, readonly string[]][]).map(([key, options]) => <fieldset key={key}><legend className="mb-2 text-sm font-semibold text-primary">{key === 'energy' ? 'Energía' : key === 'sleep' ? 'Sueño' : 'Síntomas'}</legend><div className="grid gap-2 sm:grid-cols-3">{options.map(option => <button type="button" key={option} onClick={() => setBody(current => ({ ...current, [key]: option }))} className={`rounded-xl border px-3 py-3 text-left text-sm transition hover:border-primary hover:bg-secondary ${body[key] === option ? 'border-primary bg-primary text-primary-foreground' : 'border-border text-muted-foreground'}`}>{option}</button>)}</div></fieldset>)}<label htmlFor="body-note" className="block text-sm font-semibold text-primary">Una nota, si quieres<textarea id="body-note" value={savedNote} onChange={event => setSavedNote(event.target.value)} placeholder="Por ejemplo: hoy me noto más cansado" className="mt-2 min-h-24 w-full resize-y rounded-2xl border border-border bg-background p-4 text-sm leading-6 outline-none placeholder:text-muted-foreground focus:ring-4 focus:ring-primary/10" /></label><p className="text-xs text-muted-foreground">Se guarda solo en este dispositivo y puedes cambiarlo cuando quieras.</p></div>}{tab === 'VITAL' && <VitalChat confirmedFile={confirmedFile} symptoms={symptoms} setSymptoms={setSymptoms} visitNote={visitNote} setVisitNote={setVisitNote} />}{tab === 'Historia' && <div className="mt-5 rounded-2xl bg-secondary/70 p-4 text-sm leading-6 text-primary">{confirmedFile ? `Documento confirmado: ${confirmedFile}` : 'Cuando confirmes un documento desde Conexiones, aparecerá aquí.'}</div>}{tab === 'Mi Salud' && <div className="mt-5 rounded-2xl bg-secondary/70 p-4 text-sm leading-6 text-primary">Tu resumen se construye con lo que tú añades. No hay datos inventados.</div>}<div className="mt-5 space-y-3">{(tab === 'Cuerpo' ? bodyItems : content.items).map(item => <div key={item} className="flex items-center gap-3 rounded-2xl bg-secondary/60 px-4 py-4 text-sm text-primary"><CheckCircle2 className="size-5 shrink-0 text-primary" /> {item}</div>)}</div><p className="mt-6 text-xs leading-5 text-muted-foreground">VITAL no inventa datos: esta sección se completa cuando tú añades o confirmas información.</p></div></div></div></div>
}

const MUSCLE_LABELS: Record<Muscle, { label: string; paired: boolean }> = {
  head: { label: 'Cabeza', paired: false }, neck: { label: 'Cuello / garganta', paired: false }, trapezius: { label: 'Trapecio / cervicales', paired: false },
  'upper-back': { label: 'Espalda alta', paired: false }, 'lower-back': { label: 'Espalda baja / lumbares', paired: false }, chest: { label: 'Pecho', paired: false },
  abs: { label: 'Abdomen', paired: false }, obliques: { label: 'Costado', paired: true }, 'front-deltoids': { label: 'Hombro', paired: true }, 'back-deltoids': { label: 'Hombro (detrás)', paired: true },
  biceps: { label: 'Brazo', paired: true }, triceps: { label: 'Brazo (detrás)', paired: true }, forearm: { label: 'Antebrazo / muñeca', paired: true },
  adductor: { label: 'Ingle / cara interna del muslo', paired: true }, abductors: { label: 'Cadera', paired: true }, quadriceps: { label: 'Muslo', paired: true },
  hamstring: { label: 'Muslo (detrás)', paired: true }, gluteal: { label: 'Glúteo', paired: true }, knees: { label: 'Rodilla', paired: true },
  calves: { label: 'Gemelo / pantorrilla', paired: true }, 'left-soleus': { label: 'Tobillo izquierdo', paired: false }, 'right-soleus': { label: 'Tobillo derecho', paired: false },
}
const EXTRA_ZONES: { label: string; paired: boolean }[] = [
  { label: 'Ojos', paired: false }, { label: 'Oído', paired: true }, { label: 'Boca / dientes', paired: false }, { label: 'Corazón', paired: false },
  { label: 'Pulmones / respiración', paired: false }, { label: 'Estómago / digestión', paired: false }, { label: 'Mano', paired: true }, { label: 'Pie', paired: true }, { label: 'Piel', paired: false },
]

function BodyVisual({ body, symptoms, setSymptoms }: { body: { energy: string; sleep: string; symptoms: string }; symptoms: VitalSymptom[]; setSymptoms: (next: SetStateAction<VitalSymptom[]>) => void }) {
  const [view, setView] = useState<'anterior' | 'posterior'>('anterior')
  const [picked, setPicked] = useState<{ label: string; paired: boolean; muscle?: Muscle } | null>(null)
  const [side, setSide] = useState<'derecha' | 'izquierda' | 'ambos'>('ambos')
  const [composerOpen, setComposerOpen] = useState(false)
  const [intensity, setIntensity] = useState('leve')
  const [symptomNote, setSymptomNote] = useState('')
  const completed = [Boolean(body.energy), Boolean(body.sleep), Boolean(body.symptoms) || symptoms.length > 0].filter(Boolean).length
  const [todayTimestamp] = useState(() => Date.now())
  const recentSymptoms = symptoms.filter(item => {
    const age = todayTimestamp - new Date(item.fecha).getTime()
    return age >= 0 && age <= 90 * 24 * 60 * 60 * 1000
  })
  const zoneHasSymptoms = (label: string) => recentSymptoms.some(item => item.zona === label || item.zona.startsWith(`${label} · `))
  const selectedZone = picked ? (picked.paired && side !== 'ambos' ? `${picked.label} · ${side}` : picked.label) : ''
  const markedMuscles = (Object.keys(MUSCLE_LABELS) as Muscle[]).filter(muscle => zoneHasSymptoms(MUSCLE_LABELS[muscle].label))
  const data: IExerciseData[] = [
    ...(markedMuscles.length ? [{ name: 'Con registro', muscles: markedMuscles, frequency: 1 }] : []),
    ...(picked?.muscle ? [{ name: 'Seleccionada', muscles: [picked.muscle], frequency: 2 }] : []),
  ]
  const pick = (zone: { label: string; paired: boolean; muscle?: Muscle }) => { setPicked(zone); setSide('ambos'); setComposerOpen(false) }
  const saveSymptom = () => {
    if (!selectedZone) return
    setSymptoms(current => [...current, { zona: selectedZone, fecha: new Date().toISOString(), intensidad: intensity, nota: symptomNote.trim() }])
    setSymptomNote('')
    setComposerOpen(false)
  }
  return (
    <div className="overflow-hidden rounded-[2rem] border border-primary/15 bg-secondary/45 p-4 shadow-sm sm:p-6">
      <div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-primary">Tu cuerpo</p><p className="mt-2 text-base leading-7 text-muted-foreground">Toca la zona donde notas algo.</p></div><span className="rounded-full bg-background px-3 py-1.5 text-xs font-semibold text-primary">{completed}/3 registrados</span></div>
      <div className="mt-5 grid grid-cols-2 gap-2 rounded-2xl bg-background p-1">{([['anterior', 'De frente'], ['posterior', 'De espaldas']] as const).map(([value, label]) => <button type="button" key={value} onClick={() => setView(value)} className={`h-12 rounded-xl text-base font-semibold transition ${view === value ? 'bg-primary text-primary-foreground' : 'text-primary hover:bg-secondary'}`}>{label}</button>)}</div>
      <div className="mt-5 grid justify-items-center rounded-[1.5rem] bg-background/80 px-4 py-6">
        <Model type={view} data={data} bodyColor="#D8D2C4" highlightedColors={['#C9A227', '#2E5E4E', '#2E5E4E']} onClick={({ muscle }: IMuscleStats) => pick({ ...MUSCLE_LABELS[muscle], muscle })} style={{ width: '100%', maxWidth: '15rem' }} svgStyle={{ cursor: 'pointer' }} />
        <p className="mt-4 flex items-center gap-4 text-xs text-muted-foreground"><span className="flex items-center gap-1.5"><span className="size-3 rounded-full bg-[#C9A227]" /> Con registro</span><span className="flex items-center gap-1.5"><span className="size-3 rounded-full bg-[#2E5E4E]" /> Seleccionada</span></p>
      </div>
      <p className="mt-5 text-sm font-semibold text-primary">Otras zonas</p>
      <div className="mt-2 flex flex-wrap gap-2">{EXTRA_ZONES.map(zone => <button type="button" key={zone.label} onClick={() => pick(zone)} className={`rounded-full border px-4 py-2.5 text-sm font-semibold transition ${picked?.label === zone.label ? 'border-primary bg-primary text-primary-foreground' : zoneHasSymptoms(zone.label) ? 'border-[#C9A227] bg-[#C9A227]/15 text-primary' : 'border-border bg-background text-primary hover:bg-secondary'}`}>{zone.label}</button>)}</div>
      {picked && <div className="mt-5 rounded-[2rem] border border-border bg-card p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-primary">Zona seleccionada</p><h3 className="mt-1 font-serif text-3xl text-primary">{selectedZone}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{zoneHasSymptoms(picked.label) ? 'Tienes un registro reciente en esta zona.' : 'Todavía no hay nada registrado aquí.'}</p></div><button type="button" onClick={() => setPicked(null)} aria-label="Cerrar ficha" className="text-muted-foreground hover:text-primary"><X className="size-6" /></button></div>
        {picked.paired && <div className="mt-4"><p className="text-sm font-semibold text-primary">¿Qué lado? (el tuyo)</p><div className="mt-2 grid grid-cols-3 gap-2">{(['derecha', 'izquierda', 'ambos'] as const).map(option => <button type="button" key={option} onClick={() => setSide(option)} className={`h-12 rounded-xl border text-sm font-semibold capitalize ${side === option ? 'border-primary bg-primary text-primary-foreground' : 'border-border text-primary hover:bg-secondary'}`}>{option}</button>)}</div></div>}
        <button type="button" onClick={() => setComposerOpen(true)} className="mt-5 h-14 w-full rounded-full bg-primary text-base font-semibold text-primary-foreground hover:opacity-90">Anotar qué noto aquí</button>
        {composerOpen && <div className="mt-5 border-t border-border pt-5"><p className="text-sm font-semibold text-primary">¿Cómo de intenso es?</p><div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-5">{['leve', 'molesto', 'fuerte', 'muy fuerte', 'insoportable'].map(option => <button type="button" key={option} onClick={() => setIntensity(option)} className={`rounded-xl border px-2 py-3 text-sm font-semibold capitalize ${intensity === option ? 'border-primary bg-primary text-primary-foreground' : 'border-border text-muted-foreground hover:bg-secondary'}`}>{option}</button>)}</div><label className="mt-4 block text-sm font-semibold text-primary">Nota<textarea value={symptomNote} onChange={event => setSymptomNote(event.target.value)} placeholder="Describe brevemente qué notas" className="mt-2 min-h-24 w-full resize-y rounded-2xl border border-border bg-background p-4 text-base font-normal outline-none focus:ring-4 focus:ring-primary/10" /></label><button type="button" onClick={saveSymptom} className="mt-4 h-14 w-full rounded-full bg-primary text-base font-semibold text-primary-foreground">Guardar</button></div>}
      </div>}
      <div className="mt-5 grid gap-3 sm:grid-cols-3"><BodyStatus label="Energía" value={body.energy || 'Pendiente'} ready={Boolean(body.energy)} icon={<Activity className="size-4" />} /><BodyStatus label="Sueño" value={body.sleep || 'Pendiente'} ready={Boolean(body.sleep)} icon={<Moon className="size-4" />} /><BodyStatus label="Síntomas" value={body.symptoms || (symptoms.length ? `${symptoms.length} registrados` : 'Ninguno añadido')} ready={Boolean(body.symptoms) || symptoms.length > 0} icon={<HeartPulse className="size-4" />} /></div>
      <div className="mt-5 rounded-2xl bg-background/75 px-4 py-3 text-xs leading-5 text-muted-foreground"><span className="font-semibold text-primary">Sin diagnóstico.</span> Este mapa solo te ayuda a observarte y preparar una conversación con tu profesional.</div>
    </div>
  )
}

const VITAL_SYSTEM_PROMPT = `Eres el compañero de salud de VITAL OS. Hablas en castellano, cercano, tranquilo, claro, frases cortas. Nunca alarmas, nunca culpabilizas.
Puedes: explicar medicamentos en lenguaje sencillo, avisar de interacciones conocidas, recordar quién es cada médico y para qué, ayudar a preparar una consulta, explicar un informe, ayudar a describir un síntoma, sugerir cuándo pedir cita.
No diagnosticas, no cambias ni retiras medicación, no sustituyes al médico.
Ante síntomas de emergencia (dolor en el pecho, dificultad para respirar, pérdida de fuerza en un lado, confusión repentina) tu primera frase siempre es: llama al 112.
No inventas datos del usuario que no estén en el contexto que te paso. Si no sabes algo, lo dices.
Cierra alguna vez con: "Poquet a poquet."`

type VitalSymptom = { zona: string; fecha: string; intensidad: string; nota: string }
type ChatMessage = { rol: 'usuario' | 'vital'; texto: string; fecha: string }

function VitalChat({ confirmedFile, symptoms, setSymptoms, visitNote, setVisitNote }: { confirmedFile: string | null; symptoms: VitalSymptom[]; setSymptoms: (next: SetStateAction<VitalSymptom[]>) => void; visitNote: string; setVisitNote: (next: SetStateAction<string>) => void }) {
  const [messages, setMessages] = useStoredState<ChatMessage[]>('vital-chat-messages', [])
  const [input, setInput] = useState('')
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState('')
  const [pendingAction, setPendingAction] = useState<{ kind: 'symptom' | 'visit'; text: string } | null>(null)
  const [medications] = useStoredState('vital-medications', ['Enalapril · 8:00', 'Vitamina D · 13:00'])
  const [emergencyProfile] = useStoredState('vital-emergency-profile', { allergies: 'Ninguna registrada' })
  const context = `Medicaciones: ${medications.join(', ')}\nAlergias: ${emergencyProfile.allergies}\nSíntomas de los últimos 90 días: ${symptoms.map(item => `${item.zona} (${item.intensidad}): ${item.nota}`).join('; ') || 'ninguno registrado'}\nDocumento confirmado: ${confirmedFile || 'ninguno'}\nPróxima cita: Dra. Laura Soler, 23 septiembre, 10:30, Medicina de familia · CAP Les Corts\nNota de visita guardada: ${visitNote || 'ninguna'}`
  const sendMessage = async (value = input) => {
    const text = value.trim()
    if (!text || loading) return
    const { blink } = await import('@/blink/client')
    if (!blink.auth.isAuthenticated()) { setError('Para hablar con la IA, entra en VITAL. Tus datos locales no se moverán de este dispositivo.'); return }
    const userMessage: ChatMessage = { rol: 'usuario', texto: text, fecha: new Date().toISOString() }
    const nextMessages = [...messages, userMessage]
    setMessages(nextMessages); setInput(''); setError(''); setLoading(true)
    try {
      const history = nextMessages.slice(-10).map(message => ({ role: message.rol === 'usuario' ? 'user' as const : 'assistant' as const, content: message.texto }))
      const response = await blink.ai.generateText({ model: 'openai/gpt-4.1-mini', messages: [{ role: 'system', content: `${VITAL_SYSTEM_PROMPT}\n\nCONTEXTO ACTUAL DEL USUARIO:\n${context}` }, ...history], maxTokens: 420, temperature: 0.4 })
      setMessages(current => [...current, { rol: 'vital', texto: response.text, fecha: new Date().toISOString() }])
      const combined = `${text} ${response.text}`.toLowerCase()
      if (/(dolor|molestia|síntoma|mareo|náusea|cansancio|fiebre|respirar|pecho)/.test(combined)) setPendingAction({ kind: 'symptom', text })
      else if (/(cita|pregunta|médico|doctora|consulta|apunta esto)/.test(combined)) setPendingAction({ kind: 'visit', text: response.text })
    } catch (caught) { setError(caught instanceof Error ? caught.message : 'No he podido conectar con la IA.') } finally { setLoading(false) }
  }
  const signInForChat = async () => { const { blink } = await import('@/blink/client'); blink.auth.login(window.location.href) }
  const confirmAction = () => { if (!pendingAction) return; if (pendingAction.kind === 'visit') setVisitNote(current => current ? `${current}\n${pendingAction.text}` : pendingAction.text); else setSymptoms(current => [...current, { zona: 'Síntoma descrito en VITAL', fecha: new Date().toISOString(), intensidad: 'molesto', nota: pendingAction.text }]); setPendingAction(null) }
  return <div className="mt-5 overflow-hidden rounded-[2rem] border border-border bg-card shadow-sm"><div className="border-b border-border bg-secondary/60 p-5"><p className="text-xs font-bold uppercase tracking-[0.18em] text-primary">Conversación VITAL</p><p className="mt-2 text-sm leading-6 text-muted-foreground">Tu compañero de salud habla contigo con calma. Para usar la IA, entra en VITAL; el resto de tu espacio sigue siendo local.</p></div><div className="max-h-[32rem] min-h-56 space-y-3 overflow-y-auto p-5">{messages.length === 0 && <div className="rounded-2xl bg-secondary p-4 text-sm leading-6 text-primary">Hola. Puedo ayudarte a entender tu medicación, preparar tu cita o describir lo que notas. ¿Por dónde empezamos?</div>}{messages.map((message, index) => <div key={`${message.fecha}-${index}`} className={`max-w-[88%] whitespace-pre-line rounded-2xl px-4 py-3 text-sm leading-6 ${message.rol === 'usuario' ? 'ml-auto bg-primary text-primary-foreground' : 'bg-secondary text-primary'}`}>{message.texto}</div>)}{loading && <div className="w-fit rounded-2xl bg-secondary px-4 py-3 text-sm text-primary">VITAL está pensando...</div>}{pendingAction && <div className="rounded-2xl border border-border bg-background p-4"><p className="text-sm font-semibold text-primary">¿Lo apunto así?</p><p className="mt-2 text-sm leading-6 text-muted-foreground">{pendingAction.text}</p><div className="mt-3 flex gap-2"><button type="button" onClick={confirmAction} className="rounded-full bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">Sí</button><button type="button" onClick={() => setPendingAction(null)} className="rounded-full border border-border px-4 py-2 text-sm font-semibold text-primary">No</button></div></div>}</div>{error && <div className="mx-5 mb-4 rounded-2xl border border-accent/50 bg-accent/20 p-4 text-sm leading-6 text-accent-foreground"><p>{error}</p>{error.includes('sesión') || error.includes('hablar con la IA') ? <button type="button" onClick={() => void signInForChat()} className="mt-3 rounded-full bg-primary px-4 py-2 font-semibold text-primary-foreground">Entrar en VITAL</button> : null}</div>}<form onSubmit={event => { event.preventDefault(); void sendMessage() }} className="flex gap-2 border-t border-border p-4"><input value={input} onChange={event => setInput(event.target.value)} placeholder="Escribe a VITAL..." className="min-w-0 flex-1 rounded-full border border-border bg-background px-4 py-3 text-sm outline-none focus:ring-4 focus:ring-primary/10" aria-label="Mensaje para VITAL" /><button type="submit" disabled={loading} className="rounded-full bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground disabled:opacity-50">Enviar</button></form></div>
}

function BodyStatus({ label, value, ready, icon }: { label: string; value: string; ready: boolean; icon: ReactNode }) { return <div className="flex items-center gap-3 rounded-2xl border border-border bg-background/75 px-3 py-3"><span className={`grid size-9 place-items-center rounded-xl ${ready ? 'bg-primary text-primary-foreground' : 'bg-secondary text-primary'}`}>{icon}</span><div className="min-w-0"><p className="text-xs text-muted-foreground">{label}</p><p className="truncate text-sm font-semibold text-primary">{value}</p></div></div> }

export const Route = createFileRoute('/app/')({
  head: () => ({ meta: [{ title: 'Hoy · VITAL OS' }, { name: 'description', content: 'Tu salud, contigo. Poquet a poquet.' }] }),
  component: VitalAppRoute,
})

function VitalAppRoute() { return <BlinkClientBoundary fallback={<AppLoading />}><VitalApp /></BlinkClientBoundary> }
function AppLoading() { return <main suppressHydrationWarning className="grid min-h-dvh place-items-center bg-background"><div suppressHydrationWarning className="text-center"><span suppressHydrationWarning className="mx-auto grid size-14 place-items-center rounded-full bg-primary text-primary-foreground"><Leaf className="size-7" /></span><p suppressHydrationWarning className="mt-4 font-serif text-2xl text-primary">VITAL</p><p suppressHydrationWarning className="mt-2 text-sm text-muted-foreground">Preparando tu espacio...</p></div></main> }

function VitalApp() {
  const [activeTab, setActiveTab] = useState<Tab>('Hoy')
  const [done, setDone] = useStoredState('vital-medication-done', false)
  const [note, setNote] = useStoredState('vital-daily-note', '')
  const firstVisit = useSyncExternalStore(vitalSeenSubscribe, getFirstVisit, getServerFirstVisit)
  const [emergencyOpen, setEmergencyOpen] = useState(false)
  const [menuOpen, setMenuOpen] = useState(false)
  const [searchOpen, setSearchOpen] = useState(false)
  const [query, setQuery] = useState('')
  const [meds, setMeds] = useStoredState('vital-medications', ['Enalapril · 8:00', 'Vitamina D · 13:00'])
  const [body] = useStoredState('vital-body-checkin', { energy: '', sleep: '', symptoms: '' })
  const start = () => { localStorage.setItem('vital-demo-seen', '1'); window.dispatchEvent(new Event('vital-demo-seen')) }
  const reset = () => { for (const key of ['vital-demo-seen', 'vital-medication-done', 'vital-daily-note', 'vital-body-checkin', 'vital-medications', 'vital-confirmed-document', 'vital-emergency-profile', 'vital-symptoms', 'vital-chat-messages', 'vital-visit-note']) localStorage.removeItem(key); window.location.reload() }
  if (firstVisit) return <Onboarding onStart={start} />
  return <main className="min-h-dvh bg-background pb-24 lg:pb-8"><header className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 lg:px-10"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-full bg-primary text-primary-foreground"><Leaf className="size-6" /></span><div><p className="font-serif text-xl font-bold text-primary">VITAL</p><p className="text-xs text-muted-foreground">Poquet a poquet</p></div></div><div className="flex items-center gap-2"><button type="button" onClick={() => setSearchOpen(value => !value)} className="grid size-11 place-items-center rounded-full text-primary hover:bg-secondary" aria-label="Buscar"><Search className="size-5" /></button><button type="button" onClick={() => setEmergencyOpen(true)} className="flex h-11 items-center gap-2 rounded-full border border-accent/60 bg-accent/20 px-4 font-semibold text-accent-foreground"><ShieldAlert className="size-5" /><span className="hidden sm:inline">Ayuda</span></button><button type="button" onClick={() => setMenuOpen(value => !value)} className="grid size-11 place-items-center rounded-full text-primary hover:bg-secondary" aria-label="Menú"><Menu className="size-5" /></button></div></header>{searchOpen && <div className="mx-5 mb-5 rounded-2xl border border-border bg-card p-4 shadow-md sm:mx-auto sm:max-w-7xl"><div className="flex items-center gap-3"><Search className="size-5 text-primary" /><input autoFocus value={query} onChange={event => setQuery(event.target.value)} placeholder="Buscar en tu espacio VITAL..." className="w-full bg-transparent text-sm outline-none" aria-label="Buscar" /></div>{query && <button type="button" onClick={() => { setActiveTab(query.toLowerCase().includes('cuerpo') ? 'Cuerpo' : 'VITAL'); setSearchOpen(false) }} className="mt-3 w-full rounded-xl bg-secondary px-4 py-3 text-left text-sm text-primary">Abrir resultado relacionado con “{query}”</button>}</div>}{menuOpen && <div className="mx-5 mb-5 rounded-2xl border border-border bg-card p-4 shadow-md sm:mx-auto sm:max-w-7xl"><p className="font-semibold text-primary">Tu espacio VITAL</p><p className="mt-1 text-sm text-muted-foreground">Todo funciona sin cuenta y se guarda en este dispositivo.</p><button type="button" onClick={reset} className="mt-3 text-xs font-semibold text-primary underline underline-offset-4 hover:text-accent-foreground">Restablecer este espacio</button></div>}<div className="mx-auto max-w-7xl px-5 lg:px-10"><div className="mb-8"><p className="text-base font-medium text-muted-foreground">Bon dia, Marc</p><h1 className="mt-1 font-serif text-4xl leading-tight text-primary sm:text-5xl">¿Cómo estás hoy?</h1></div><div className="grid gap-5 lg:grid-cols-[1.18fr_0.82fr]"><section className="rounded-[2rem] bg-primary p-6 text-primary-foreground shadow-lg sm:p-8"><p className="text-sm text-primary-foreground/70">Lo más importante ahora</p><h2 className="mt-3 font-serif text-3xl sm:text-4xl">Tu medicación de la mañana</h2><p className="mt-3 text-base leading-7 text-primary-foreground/75">Un pequeño paso para cuidar de ti. Sin prisas.</p><Button onClick={() => setDone(true)} className="mt-7 h-14 rounded-full bg-accent px-6 text-base text-accent-foreground hover:bg-accent/85">{done ? <><Check className="size-5" /> Hecho. Ya está.</> : <>Marcar como tomada <Check className="size-5" /></>}</Button></section><section className="rounded-[2rem] border border-border bg-card p-6 shadow-sm"><h2 className="font-serif text-2xl text-primary">¿Cómo te sientes?</h2><p className="mt-2 text-sm text-muted-foreground">Solo una palabra, si quieres.</p><div className="mt-5 flex justify-between">{['Tranquilo', 'Bien', 'Regular', 'Cansado', 'Mal'].map(face => <button key={face} type="button" onClick={() => setNote(face)} className={`rounded-xl p-2 text-xs ${note === face ? 'bg-secondary text-primary' : 'text-muted-foreground hover:bg-secondary'}`}>{face}</button>)}</div>{note && <p className="mt-5 rounded-xl bg-secondary px-4 py-3 text-sm text-primary">Gracias por contármelo.</p>}</section></div><div className="mt-5 grid gap-5 md:grid-cols-2"><section className="rounded-[2rem] border border-border bg-card p-6 shadow-sm"><div className="flex items-center justify-between"><h2 className="font-serif text-2xl text-primary">Tus tomas de hoy</h2><Pill className="size-6 text-accent" /></div><div className="mt-5 space-y-3">{meds.map((med, index) => <div key={`${med}-${index}`} className="flex items-center justify-between rounded-2xl bg-secondary/60 px-4 py-4"><div><p className="font-semibold text-primary">{med.split(' · ')[0]}</p><p className="text-sm text-muted-foreground">{med.split(' · ')[1]}</p></div><button type="button" onClick={() => setMeds(current => current.filter((_, itemIndex) => itemIndex !== index))} className="grid size-10 place-items-center rounded-full bg-background text-primary" aria-label={`Marcar ${med} como tomada`}><Check className="size-5" /></button></div>)}</div><button type="button" onClick={() => setMeds(current => [...current, 'Nueva medicación · Ahora'])} className="mt-4 flex h-12 w-full items-center justify-center gap-2 rounded-xl border border-dashed border-border text-sm font-semibold text-primary hover:bg-secondary"><Plus className="size-4" /> Añadir una toma</button></section><section className="rounded-[2rem] border border-border bg-card p-6 shadow-sm"><h2 className="font-serif text-2xl text-primary">Tu próxima cita</h2><div className="mt-5 rounded-2xl bg-secondary/70 p-5"><p className="text-sm text-muted-foreground">23 septiembre · 10:30</p><p className="mt-2 text-xl font-semibold text-primary">Dra. Laura Soler</p><p className="mt-1 text-sm text-muted-foreground">Medicina de familia · CAP Les Corts</p><button type="button" onClick={() => setActiveTab('VITAL')} className="mt-5 flex items-center gap-2 text-sm font-semibold text-primary hover:underline">Preparar mi visita <ArrowRight className="size-4" /></button></div></section></div><section className="mt-5 rounded-[2rem] border border-primary/15 bg-secondary/40 p-6 shadow-sm sm:p-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-bold uppercase tracking-[0.18em] text-primary">Cuerpo</p><h2 className="mt-2 font-serif text-3xl text-primary">Escucha cómo estás, sin presión.</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Registra tu energía, sueño o síntomas cuando te apetezca.</p></div><Button onClick={() => setActiveTab('Cuerpo')} variant="outline" className="h-12 rounded-full">Abrir sección Cuerpo <ChevronRight className="size-5" /></Button></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><button type="button" onClick={() => setActiveTab('Cuerpo')} className="rounded-2xl border border-border bg-background p-4 text-left"><p className="text-sm font-semibold text-primary">Energía</p><p className="mt-3 font-serif text-2xl text-primary">{body.energy || 'Sin registrar'}</p></button><button type="button" onClick={() => setActiveTab('Cuerpo')} className="rounded-2xl border border-border bg-background p-4 text-left"><p className="text-sm font-semibold text-primary">Sueño</p><p className="mt-3 font-serif text-2xl text-primary">{body.sleep || 'Sin registrar'}</p></button><button type="button" onClick={() => setActiveTab('Cuerpo')} className="rounded-2xl border border-border bg-background p-4 text-left"><p className="text-sm font-semibold text-primary">Síntomas</p><p className="mt-3 font-serif text-2xl text-primary">{body.symptoms || 'Ninguno'}</p></button></div></section></div><nav className="fixed inset-x-0 bottom-0 z-30 border-t border-border bg-background/95 px-2 py-2 backdrop-blur lg:static lg:mx-auto lg:mt-8 lg:max-w-7xl lg:border-t-0 lg:bg-transparent lg:px-10"><div className="mx-auto grid max-w-2xl grid-cols-6 gap-1 lg:rounded-2xl lg:border lg:border-border lg:bg-card lg:p-2">{tabs.map(({ name, icon: Icon }) => <button key={name} type="button" onClick={() => setActiveTab(name)} className={`flex min-h-[58px] flex-col items-center justify-center gap-1 rounded-xl text-[11px] font-semibold transition ${activeTab === name ? 'bg-primary text-primary-foreground' : 'text-muted-foreground hover:bg-secondary hover:text-primary'}`}><Icon className="size-5" /><span>{name}</span></button>)}</div></nav>{emergencyOpen && <Emergency onClose={() => setEmergencyOpen(false)} />}{activeTab === 'Yo' && !emergencyOpen && <ConnectionsScreen onClose={() => setActiveTab('Hoy')} />}{activeTab !== 'Hoy' && activeTab !== 'Yo' && !emergencyOpen && <TabPanel tab={activeTab} onBack={() => setActiveTab('Hoy')} />}</main>
}

function Onboarding({ onStart }: { onStart: () => void }) { return <main className="flex min-h-dvh items-center justify-center bg-background px-6 py-10"><div className="w-full max-w-lg text-center"><span className="mx-auto grid size-20 place-items-center rounded-full bg-primary text-primary-foreground shadow-lg"><Leaf className="size-10" /></span><p className="mt-8 text-sm font-bold uppercase tracking-[0.2em] text-primary">VITAL OS</p><h1 className="mt-5 font-serif text-5xl leading-tight text-primary sm:text-6xl">Bon dia.<br />Soy VITAL.</h1><p className="mx-auto mt-6 max-w-md text-lg leading-8 text-muted-foreground">Tu compañero de salud. Vamos a empezar con calma, solo con lo que quieras compartir.</p><Button onClick={onStart} size="lg" className="mt-9 h-14 rounded-full px-8 text-base">Empezar, sin cuenta <ArrowRight className="size-5" /></Button><p className="mt-5 text-sm text-muted-foreground">Puedes saltarte cualquier paso. Poquet a poquet.</p></div></main> }

function Emergency({ onClose }: { onClose: () => void }) { const [profile, setProfile] = useStoredState('vital-emergency-profile', { name: 'Marc', birthYear: '1972', allergies: 'Ninguna registrada', contact: 'Anna · 600 000 000' }); const [editing, setEditing] = useState(false); const [meds] = useStoredState('vital-medications', ['Enalapril · 8:00', 'Vitamina D · 13:00']); return <div className="fixed inset-0 z-50 grid place-items-center bg-primary/20 px-5 backdrop-blur-sm"><div className="w-full max-w-md rounded-[2rem] border border-border bg-background p-6 shadow-lg"><div className="flex items-start justify-between"><div><ShieldAlert className="size-7 text-accent-foreground" /><h2 className="mt-4 font-serif text-3xl text-primary">Tarjeta de emergencia</h2></div><button type="button" onClick={onClose} aria-label="Cerrar"><X /></button></div>{editing ? <div className="mt-6 space-y-4"><ProfileField label="Nombre" value={profile.name} onChange={value => setProfile(current => ({ ...current, name: value }))} /><ProfileField label="Alergias" value={profile.allergies} onChange={value => setProfile(current => ({ ...current, allergies: value }))} /><ProfileField label="Contacto" value={profile.contact} onChange={value => setProfile(current => ({ ...current, contact: value }))} /><button type="button" onClick={() => setEditing(false)} className="h-12 w-full rounded-full bg-primary font-semibold text-primary-foreground">Guardar datos</button></div> : <><div className="mt-6 space-y-3 text-sm"><p><strong>Nombre:</strong> {profile.name}</p><p><strong>Alergias:</strong> {profile.allergies}</p><p><strong>Medicaciones:</strong> {meds.join(', ')}</p><p><strong>Contacto:</strong> {profile.contact}</p></div><div className="mt-7 grid gap-3"><a href="tel:112" className="flex h-14 items-center justify-center rounded-full bg-primary font-semibold text-primary-foreground">Llamar al 112</a><button type="button" onClick={() => setEditing(true)} className="h-12 rounded-full border border-border font-semibold text-primary">Editar mis datos</button></div></>}<p className="mt-5 text-center text-xs text-muted-foreground">Si hay una emergencia, llama al 112.</p></div></div> }
function ProfileField({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { return <label className="block text-sm font-semibold text-primary">{label}<input value={value} onChange={event => onChange(event.target.value)} className="mt-2 h-12 w-full rounded-xl border border-border bg-background px-4 font-normal outline-none focus:ring-4 focus:ring-primary/10" /></label> }

function ConnectionsScreen({ onClose }: { onClose: () => void }) { const [selectedFile, setSelectedFile] = useState<string | null>(null); const [selectedSource, setSelectedSource] = useState(''); const [reviewOpen, setReviewOpen] = useState(false); const [confirmedFile, setConfirmedFile] = useStoredState<string | null>('vital-confirmed-document', null); const choose = (event: React.ChangeEvent<HTMLInputElement>, source: string) => { const file = event.target.files?.[0]; if (!file) return; setSelectedFile(file.name); setSelectedSource(source); setReviewOpen(true) }; return <section className="fixed inset-0 z-10 overflow-y-auto bg-background pb-28 pt-5"><div className="mx-auto max-w-5xl px-5 lg:px-10"><button type="button" onClick={onClose} className="mb-8 flex items-center gap-2 text-sm font-semibold text-primary"><ArrowRight className="size-4 rotate-180" /> Volver a Hoy</button><p className="text-sm font-bold uppercase tracking-[0.2em] text-primary">Tu espacio</p><h1 className="mt-2 font-serif text-4xl text-primary sm:text-5xl">Conexiones</h1><p className="mt-3 max-w-2xl text-base leading-7 text-muted-foreground">Aquí decides qué entra en VITAL. Nada se conecta solo.</p><div className="mt-8 grid gap-4 sm:grid-cols-2"><ConnectionCard icon={<HeartPulse className="size-6" />} title="Apple Salud" status="Importación manual" tone="amber"><p>Selecciona una captura o PDF y revísalo antes de confirmar.</p><label className="mt-4 flex h-12 cursor-pointer items-center justify-center gap-2 rounded-xl bg-primary text-sm font-semibold text-primary-foreground"><Upload className="size-4" /> Elegir captura o PDF<input type="file" accept="image/*,.pdf" onChange={event => choose(event, 'Apple Salud')} className="sr-only" /></label>{selectedFile && <p className="mt-3 text-xs text-primary">{selectedFile} · listo para revisar</p>}</ConnectionCard><ConnectionCard icon={<Weight className="size-6" />} title="Withings" status="No conectado" tone="teal"><p>La autorización segura todavía no está activa. No fingimos que está conectado.</p></ConnectionCard><ConnectionCard icon={<Mail className="size-6" />} title="Portales médicos" status="Importación manual" tone="sand"><p>Sube informes, citas o analíticas y confirma cada selección.</p>{confirmedFile && <p className="mt-3 text-xs text-primary">Documento confirmado: {confirmedFile}</p>}<label className="mt-4 flex h-12 cursor-pointer items-center justify-center gap-2 rounded-xl bg-primary text-sm font-semibold text-primary-foreground"><Upload className="size-4" /> Importar documento<input type="file" accept="image/*,.pdf" onChange={event => choose(event, 'Portales médicos')} className="sr-only" /></label></ConnectionCard><ConnectionCard icon={<CalendarDays className="size-6" />} title="Calendario" status="Manual" tone="lavender"><p>VITAL no lee tu calendario sin permiso.</p></ConnectionCard></div></div>{reviewOpen && selectedFile && <div className="fixed inset-0 z-50 grid place-items-center bg-primary/20 px-5 backdrop-blur-sm"><div className="w-full max-w-md rounded-[2rem] border border-border bg-background p-6 shadow-lg"><h2 className="font-serif text-3xl text-primary">Revisar antes de guardar</h2><p className="mt-4 text-sm leading-6 text-muted-foreground">Fuente: {selectedSource}<br />Documento: {selectedFile}</p><div className="mt-6 grid gap-3"><button type="button" onClick={() => { setConfirmedFile(selectedFile); setReviewOpen(false) }} className="h-12 rounded-full bg-primary font-semibold text-primary-foreground">Confirmar selección</button><button type="button" onClick={() => setReviewOpen(false)} className="h-12 rounded-full border border-border font-semibold text-primary">Descartar documento</button></div></div></div>}</section> }
function ConnectionCard({ icon, title, status, tone, children }: { icon: ReactNode; title: string; status: string; tone: 'amber' | 'teal' | 'sand' | 'lavender'; children: ReactNode }) { const toneClass = { amber: 'bg-accent/25 text-accent-foreground', teal: 'bg-primary/10 text-primary', sand: 'bg-secondary text-primary', lavender: 'bg-primary/10 text-primary' }[tone]; return <article className="rounded-[2rem] border border-border bg-card p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md sm:p-6"><span className={`grid size-12 place-items-center rounded-2xl ${toneClass}`}>{icon}</span><h2 className="mt-5 font-serif text-2xl text-primary">{title}</h2><p className="mt-2 text-xs font-semibold text-muted-foreground">{status}</p><div className="mt-4 text-sm leading-6 text-muted-foreground">{children}</div></article> }